# Stream S4 — Affine Surgery Selection: Registration Frame

(Lead-issued frame, recorded verbatim at kickoff. P0 document:
docs/P0-formalization.md.)

*[Frame annotation (P0, lead-approved): "the one operator this
portfolio has validated as landscape surgery is the bijective one"
was a composite-operator result — see the L0 ledger annotation and
P0-formalization.md's operator split. The in-class object is the
column half; the P1 rediscovery gate is restated accordingly.]*

Question: can sparse affine transformations, selected by measured
basin-merging, be found automatically — and do they translate into
solver improvement under honest cost accounting? Hypothesis under
test: basin-merging scores can DISCOVER the right transformations
without being told the structure, on the one operator class where
merging is possible at all (the L0-validated bijective operator).

P0 (1 wk): operator class fixed (elementary x_i <- x_i XOR x_j, depth
d); transformed-formula representation for measurement vs
CNF-re-encoding for solver tests, accounted separately; anchor sanity
(basin merging along the Gaussian composition on pure XOR — the score
landscape must be walkable on the known-good case); representation
tolls measured with registered caps; null-direction theorem attempt
for pure CNF. Kill: unaffordable representation at useful depth, or
provably unwalkable anchor score landscape.

P1 (post-P0 registration): selection loop; blind vs structure-informed
candidates (registered comparison); validated sampled score with
saturation guard; random-op + identity controls (beat random maximum);
instances = pure XOR (must rediscover Gaussian elimination — the
known-proof-rediscovery gate), hidden-XOR CNF, mixed, pure-CNF
negative control.

P2 (post-P1): solver effect under L3b conservation-law accounting
(total cost incl. scoring); CryptoMiniSat-style native-XOR baseline
pre-committed: if Gaussian-integrated solvers already bank the gains,
the contribution is the selection principle, not the speedup.

Discipline inherited unchanged; 3-week box; ledger sentence owed.
