# Stream S5 — Proof-Space Geometry: Registration Frame

(Lead-issued frame, recorded verbatim at kickoff. P0 document:
docs/P0-formalization.md.)

Question: is ER's power visible as geometry in proof space — does the
short ER refutation of PHP occupy a geometrically distinguishable
region relative to the exponential resolution refutations around it?
The arc's final chapter by construction.

The known trap, named first: no canonical object. P0 kill (the
no-ending fence): if no candidate geometry survives
triviality-checking with a single defensible canonical definition
within the box, S5 closes as "proof-space geometry is not formalizable
with current tools" — a ledger sentence, not a suspension. No
candidate-shopping beyond the registered menu of three:
(1) clause-database states; (2) proof-DAG rewrite space;
(3) restricted-width/level projections (the only candidate with
existing theoretical landmarks — Ben-Sasson–Wigderson).

P0 (2 wks, paper-first): per candidate — formal definition, triviality
theorems, exact-feasibility MEASURED at PHP(3)/PHP(4). Selection: at
most ONE proceeds; tie-break pre-registered = existing landmarks win
(calibration beats novelty). Canonical altitude fixed by argument
before P1; others reported-never-adjudicated.

P1 (only after P0): anchor measurement — enumerate the refutation
region containing the known short ER artifact, minimal resolution
refutations, and the population between; tunnel-form question with
ascent budget; definition-content control (Cook vs shape-matched
random); screen-vs-dynamics two-stage mandatory for any "useful
signal" claim.

Termination: (1) verdict either way at the anchor — both final ledger
sentences; (2) jackpot pre-fenced — NO constructive follow-up inside
S5; (3) the trap — fenced by P0 kill.

Discipline inherited unchanged. The stream cannot run forever; it can
only end in one of three registered ways.
